services:
  backend:
    image: neurodatics-backend:local
    build:
      context: ./backend
      dockerfile: Dockerfile

  worker:
    image: neurodatics-backend:local
    build:
      context: ./backend
      dockerfile: Dockerfile

  frontend:
    image: neurodatics-frontend:local
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_INTERNAL_API_BASE_URL: ${NEXT_INTERNAL_API_BASE_URL:-http://backend:8000}
